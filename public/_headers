# Cloudflare Static Assets header rules.
# Filenames carry version + content hash, so contents never change under a
# given URL — safe to cache forever. Bump the filename to ship a new version.
/static/*
  Cache-Control: public, max-age=31536000, immutable
